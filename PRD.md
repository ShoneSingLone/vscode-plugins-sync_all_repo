# x\-space Toolkit —— VS Code 插件 PRD

## 1. 背景：三件套现状

```
x-space 生态下你有三个工具，各管一摊：

┌─ boundless-vue-helper ─────────────────────┐
│  别名跳转（alias）                           │
│  _.$xxx 函数定义自动扫描与跳转               │
│  代码片段（xsfc, xsfcdialog, ximv 等）      │
│  Boundless 项目自动识别                     │
└─────────────────────────────────────────────┘

┌─ sync-all-repos (git-subtree-manager) ─────┐
│  傻傻的 subtree 管理器                      │
│  质量不行，需要重写                         │
└─────────────────────────────────────────────┘

┌─ commitmsg skill (Claude Code 内) ─────────┐
│  git add . → 分析变更 → 生成规范 commit    │
│  但只能在 Claude Code 里用，VS Code 里没有  │
└─────────────────────────────────────────────┘
```

本 PRD 定义 **x\-space Toolkit** 插件：在 VS Code 底部面板提供多仓库同步、代码智能跳转与规范提交的一站式闭环。核心设计是「同步」一步动作（暂存 → 生成规范 commit → 推送目标远程）、远程一等公民、仓库扁平化动态发现。v1\.0 验收主线为模块 A\+C 融合与多远程支持；代码跳转依赖 x\-space 项目结构，非 x\-space 项目仅启用多仓库管理。

# 产品定位

## 一句话定位

x\-space 项目的一站式开发助手：管理多仓库同步、代码智能跳转、一个按钮全同步。

## 目标用户

- x\-space 生态的开发者（个人 \+ 团队）

- 使用多仓库 \+ 多远程仓库架构的团队

## 核心价值

| 价值点         | 说明                                                                                   |
| -------------- | -------------------------------------------------------------------------------------- |
| 同步是一步动作 | git add → 生成规范 commit → 推送目标远程，整条链路一步到位，无需切换终端、无需记忆命令 |
| 远程是一等公民 | 一个仓库可挂多个远程，面板完整展示每个远程的状态与差异，同步时按需选择                 |
| 动态自适应     | 仓库结构运行时自动发现，新仓库加入后刷新即出现，零配置                                 |
| 一站式入口     | 底部面板聚合仓库状态、代码跳转与快捷操作，边写代码边看状态                             |

## 面板位置

面板位于 VS Code 编辑器底部，以底部面板形态展示自绘的 x\-space 工作台，与编辑器内容同屏可见，无需切换左侧栏。该面板是插件的总入口，聚合仓库同步、代码跳转与快捷操作。

# 核心概念

## 同步（Sync）

**同步** = 提交本地改动 \+ 推送到目标远程（可选：同时拉取最新）。一个仓库可能配置多个远程（如 origin、gitee、upstream），每个远程下又有不同分支。「提交 \+ 推送」本质是同一概念：同步。每个仓库行把「同步」做成一步动作。

```text
本地工作区 → git add . → git commit → git push <远程> <分支>
                                      ↘ （可选）git pull <远程> <分支> 放入同步动作
```

## 同步模式

| 模式       | 行为                                                         |
| ---------- | ------------------------------------------------------------ |
| 单远程同步 | commit \+ push 到选定的 1 个远程                             |
| 多远程同步 | commit \+ 逐个 push 到多个选定远程（按面板勾选）             |
| 拉取后同步 | 先 fetch 指定远程，再 commit \+ push（避免覆盖他人最新代码） |
| 全部同步   | 所有脏仓库走一遍多远程同步                                   |

## 动态自适应原则

x\-space 的仓库结构是动态变化的，插件把「结构分析」当作运行时行为，而非写死的静态配置：

- `x-space.business_statics` 下会有新的业务仓库不断加入（当前约 28 个，持续增长）

- 新的 `x-space.*` 顶层组件也可能不断加入，每个有自己的 git 仓库、远程、业务结构

任何时刻都不应在代码里写死仓库数量，而是完全动态地扫描、渲染。

## 设计原则

1. 一切以 x\-space 工作区为中心 — 识别 workspace 多根仓库结构

2. 非侵入 — 不改 \.git 结构、不改项目配置

3. 远程是一等公民 — 一个仓库多远程是常态，不是特例

4. 同步是一步动作 — 提交 \+ 推送是同一件事，不拆成两个按钮

# 功能设计

## 模块 A：多仓库工作台与同步引擎（底部面板）

这是插件的核心模块。多仓库管理与同步不是两个独立功能，而是同一面板的一体两面。面板不使用原生 TreeView，而是在 VS Code 底部面板用 Webview 自绘 UI，以便：

- 完全自定义布局、配色、交互动效，贴合 x\-space 品牌视觉

- 悬停、点击、搜索、分组折叠等复杂交互不受 TreeView 约束

- 后续可以方便地扩展卡片、图表、快捷键等个性化元素

面板是 x\-space 工作台的总入口，展示当前工作区所有有 `.git` 的仓库（扁平化拍平），每个仓库条目本身就是完整的同步入口：

```text
┌──────────────────────────────────────────────────────────┐
│  x-space  工作台 ▍搜索:[____] ▍[🔃全部同步][⬇全部拉取]    │
├──────────────────────────────────────────────────────────┤
│  x-space                master  origin↑2↓1 gitee↑2↓0 脏3 │
│                          [🔃同步][⬇拉取][📋状态]          │
│  x-space.business_statics develop origin↑0↓0        脏3 │
│                          [🔃同步][⬇拉取][📋状态]          │
│  business_cib           feature/xxx origin↑0↓2       脏5 │
│                          [🔃同步][⬇拉取][📋状态]          │
│  business_zhong_liang    ...                          脏1 │
│                          [🔃同步][⬇拉取][📋状态]          │
│  ...（实际数量随业务动态增减，扁平排列）                      │
└──────────────────────────────────────────────────────────┘
```

**条目内容**：状态图标（颜色标识）\+ 仓库名 \+ 当前分支 \+ 全部远程（含各远程 ahead/behind）\+ 未提交改动数（带文件分布）。

**行内操作**：同步 / 拉取 / 状态；条目右键补充：打开终端、在资源管理器中显示、重命名远程、添加远程。

### 顶栏按钮

| 按钮        | 功能                                           |
| ----------- | ---------------------------------------------- |
| 🔄 刷新     | 重新扫描所有仓库状态                           |
| ⬇️ 全部拉取 | 对所有仓库、所有远程执行 git fetch / git pull  |
| 🔃 全部同步 | 遍历所有脏仓库，逐个走同步流程（提交 \+ 推送） |
| ＋ 添加远程 | 给指定仓库配置新的远程                         |
| 🔗 管理远程 | 查看 / 修改 / 删除各仓库的远程配置             |

### 状态检测

- 是否是 git 仓库（检查 `.git`）

- 当前分支（`git branch --show-current`）

- 全部远程（`git remote -v`，不只 origin）

- 每个远程的差异（`git rev-list --count HEAD..origin/main` 等，每个远程独立计算）

- 工作区脏状态（`git status --porcelain`）

- 是否有冲突

## 模块 C：同步引擎

同步引擎内嵌于模块 A，由仓库面板驱动，负责「暂存 → 生成规范 commit → 推送」的完整流水线。触发方式：

1. 底部面板扁平列表条目 → 点击「🔃 同步」按钮

2. 命令面板 → `x-space: 同步当前仓库`

3. 顶栏「🔃 全部同步」→ 遍历所有脏仓库逐个走同步流程

### 同步流程

```text
点击「同步」
  ↓
自动执行 git add .（暂存所有改动）
  ↓
弹出输入框，分析变更后自动生成 commit message（可编辑）
  ├─ type: feat / fix / refactor / docs / style / test / chore
  ├─ scope: 根据变更文件路径自动推断
  └─ description: 中文描述
  ↓
确认提交
  ↓
选择目标远程（默认 origin，可多选，列出仓库所有远程）
  ↓
逐个执行 git push <远程> <分支>
```

### commit message 格式与推断逻辑

格式遵循 Conventional Commits \+ 中文描述：

```text
<type>(<scope>): <中文简要描述>

<中文详细说明（可选）>
```

| 推断维度          | 逻辑                                                                      |
| ----------------- | ------------------------------------------------------------------------- |
| scope（1 级仓库） | 从变更文件路径推断，如 route\-state、sidebar                              |
| scope（2 级仓库） | 使用业务名（如 business_cib）                                             |
| type              | 从变更内容语义推断（feat / fix / refactor / docs / style / test / chore） |
| description       | 中文描述                                                                  |

## 模块 B：代码智能跳转

| 功能                | 说明                                                              |
| ------------------- | ----------------------------------------------------------------- |
| Alias 跳转          | 配置化路径别名，按 Ctrl\+Click 跳转到对应文件                     |
| \_\.$xxx 跳转       | 自动扫描 common\.ts，识别所有 \_\.$xxx 函数定义位置，支持点击跳转 |
| \_api / \_opts 跳转 | 业务文件内 \_api\.xxx、\_opts\.xxx 跳转到对应 \.vue 文件          |
| 批量别名配置        | 支持 mapping_statics 外部静态目录映射                             |
| 代码片段            | xsfc、xsfcdialog、ximv 等 x\-space 专用模板                       |

**自动扫描**：启动时自动扫描工作区根目录及各 mapping_statics 目录下的 common\.ts；监听文件变化，热更新函数索引；无需用户手动配置。

## 模块 D：快捷操作

操作入口全部收在扁平列表和命令面板，不使用文件资源管理器右键菜单。

| 入口                  | 操作                                                           |
| --------------------- | -------------------------------------------------------------- |
| 命令面板 `x-space: *` | 同步、拉取、状态、管理远程、打开终端，全部命令化               |
| 扁平列表条目右键      | 打开终端、在资源管理器中显示、重命名远程、添加远程（补充操作） |

# 技术方案

## 架构

```text
extension.ts（入口）
│
├── ActivationGuard          ← 检测当前工作区是否为 x-space 项目
│                              （检查 configs.boundless.vue.project.js 是否存在）
│
├── modules/
│   ├── workspacePanel/      ← 模块 A 的 UI：自绘 x-space 工作台面板（Webview）
│   │   ├── WorkspacePanel.ts   ← Webview 面板生命周期管理
│   │   ├── panel.html          ← 面板 HTML 骨架
│   │   ├── panel.css           ← 面板样式（x-space 品牌视觉）
│   │   ├── panel.ts            ← 面板前端逻辑（渲染扁平仓库列表、交互、消息收发）
│   │   └── messages.ts         ← Webview ↔ 扩展 消息协议定义
│   │
│   ├── repoManager/         ← 模块 A 的逻辑：多仓库管理（扁平扫描）
│   │   ├── RepoManager.ts       ← 扁平扫描（先读 folders、拍平、1 级/2 级）+ git 命令封装
│   │   ├── RepoStatus.ts        ← 状态检测逻辑（含多远程差异）
│   │   └── types.ts
│   │
│   ├── syncEngine/          ← 模块 C：同步引擎
│   │   ├── SyncEngine.ts        ← 同步流程编排（add → gen msg → commit → push）
│   │   ├── MessageGenerator.ts  ← commit message 生成（type/scope 推断）
│   │   ├── RemotePicker.ts      ← 目标远程选择 UI（多选）
│   │   └── types.ts
│   │
│   ├── codeAssistant/       ← 模块 B：代码智能跳转
│   │   ├── AliasProvider.ts     ← 别名解析与跳转
│   │   ├── CommonScanner.ts     ← common.ts 自动扫描
│   │   ├── DefinitionProvider.ts ← 定义跳转提供者
│   │   ├── CompletionProvider.ts ← 自动补全提供者
│   │   └── types.ts
│   │
│   └── output/
│       └── OutputManager.ts     ← 统一的输出日志面板
│
├── commands/                ← 命令注册
│   ├── repoCommands.ts         ← 仓库相关命令（pull / sync / remote 管理）
│   └── syncCommands.ts         ← 同步相关命令（sync / syncAll）
│
└── utils/
    ├── git.ts                  ← git 命令执行工具
    ├── config.ts               ← 配置加载与管理
    └── workspace.ts            ← 工作区工具函数
```

## 项目识别

插件通过检查工作区根目录是否存在 `configs.boundless.vue.project.js` 来判断是否是 x\-space 项目：

- 是 → 激活全部模块（仓库管理 \+ 代码跳转 \+ 同步）

- 否 → 仅激活模块 A（通用的多仓库管理功能），不干扰非 x\-space 项目

## 仓库发现策略（扁平化扫描）

### 扫描约定

仓库层级只会是 1 级或 2 级，绝不往下深入。插件在扫描和展示时都遵循这一约定，不做任何 3 级及更深的假设。只认 `.git`：有 \.git 才管，无则跳过。

### 数据来源

先读 `x-space.workspace.code-workspace` 的 `folders` 作为扫描入口，但展示时全部拍平：

```text
扫描入口：读取 x-space.workspace.code-workspace 的 folders
  ├─ folder 根有 .git   → 记为仓库（1 级）
  └─ folder 根无 .git   → 其子目录（depth=1）有 .git 才记，无则跳过
      ├─ business_cib/.git        → 记为仓库（2 级）
      └─ business_mo_ack/（无 .git）→ 跳过，不管
```

### 展示形态

- 面板不按工作目录的嵌套结构展示，而是把所有有 \.git 的仓库拍平成一个列表，一视同仁

- 不分组、纯平铺（每个仓库一个条目）；不用 TreeView 的父子层级，不用容器 / 子目录的树形

- 1 级和 2 级的仓库混在一起，平等排列，无任何层级区别

### 条目内容

- 面板条目上不显示任何角色标签（底座 / 容器 / 子仓库），只展示仓库名和仓库状态（分支、远程、未提交改动等）

- 数据来源已经是拍平的，1 级和 2 级不再有区分意义，标签是多余的

- 新仓库加入后，只要符合 1 级 / 2 级约定，刷新即自动出现在扁平列表中

### 排除规则

排除 `node_modules`、`.claude/worktrees`、归档目录等，始终忽略。

## 远程仓库处理

每个仓库的远程是其核心状态，面板必须完整展示：

```text
git remote -v
git remote get-url <name>            ← 逐个远程获取 URL
git rev-list --count HEAD..<remote>/<branch>   ← 每个远程独立计算 ahead/behind
```

同步时，用户可以在远程选择器（RemotePicker）中：

- 勾选多个目标远程，各自独立推送

- 记忆上次选择（workspaceState 缓存），下次默认复用

## 技术栈

- TypeScript

- VS Code Extension API（TreeView、Languages、StatusBar）

- child_process\.exec 执行 git 命令

- 无额外 npm 依赖（lodash 等替换为原生实现）

# 典型使用流程

以一次完整的开发动作为例，看各模块如何形成一条「同步」流水线：

```text
在 x-space.business_statics 写代码
  ↓
底部面板看到 🟡 x-space.business_statics 状态：
    ⚡ 有 3 个未提交改动
    origin ↑0 ↓0，gitee ↑0 ↓2（落后 2 个提交，该拉取了）
  → 一眼知道该同步了
  ↓
点击该行「🔃 同步」按钮（同步模式选「拉取后同步」）
  → 从 gitee 拉取最新
  → 自动执行 git add .
  → 分析变更内容，弹出输入框预填：feat(statics): 新增数据处理模块
  → 确认 / 微调，点击提交
  → 选择目标远程：origin（+ 可选 gitee）
  → 自动 git push
  ↓
面板状态刷新为：🟢 已同步
```

这就是插件的核心价值：仓库检测、规范提交、多远程推送融合为一条「同步」流水线，全程不需要切换终端。

# UI/UX 规范

- 底部面板 Tab 图标：x\-space 品牌图标（🅧 或自定义 SVG）

- 使用 Webview 自绘 UI，完全控制布局和视觉风格

- 状态颜色遵循 VS Code 主题色 token（远程落后用 warning 色，冲突用 error 色）

- 进度条：批量操作时底部状态栏显示实时进度

- 同步按钮使用 $\(sync\) 图标，与「同步」概念强关联

- 底部面板与编辑器同屏可见，实现边写代码边看仓库状态的流畅体验

# 发布规划

## v1\.0（MVP）

- 模块 A \+ C 融合：多仓库工作台 × 同步引擎（底部面板，每个仓库行含「同步」「拉取」「状态」行内按钮）

- 多远程支持：每个仓库展示全部远程，同步可选择目标远程（多选）

- 模块 B：代码智能跳转（alias \+ \_\.$xxx \+ 代码片段）

- 模块 D：命令面板化快捷操作（同步 / 拉取 / 状态 / 管理远程 / 打开终端）

## v1\.5

- 模块 A：仓库过滤搜索、状态自动刷新

- 模块 C：AI commit message 生成（调用 Claude API）

- 「拉取后同步」策略增强（冲突提醒）

- 远程管理增强（重命名、删除、fetch 全部远程）

## v2\.0

- 批量同步全部仓库（顶栏一键遍历所有脏仓库 \+ 各远程）

- 自定义同步流程（pre\-commit hooks、自定义 scope）

- 仓库历史可视化（简单图表展示各仓库提交频率）

# FAQ

## 一个仓库有多个远程，同步时会推到哪里？

同步时弹出远程选择器，默认 origin，但可以勾选多个远程逐一推送。选择会被记住，下次默认复用。

## 同步到多个远程时，某个远程失败怎么办？

逐个远程独立执行，每个远程单独报告成功 / 失败。一个失败了不影响其他远程的推送。

## commit message 的 AI 生成需要联网吗？

v1\.0 用规则模板生成（如「feat: 新增 xxx 功能」），可离线使用基础功能；v1\.5 再加 AI 调用。

## 非 x\-space 项目能用吗？

多仓库管理功能（模块 A）通用可用；代码跳转和智能同步需要 x\-space 项目结构。

## alias 配置会丢失吗？

不会。配置格式保持不变，仍然使用 `configs.boundless.vue.project.js`。

> （注：部分内容可能由 AI 生成）
