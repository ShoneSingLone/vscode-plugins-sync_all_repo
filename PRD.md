帮我开发一个 VS Code 扩展插件，兼容 Trae AI 编辑器，功能是可视化管理 Git Subtree，需求如下：

1. 界面布局
- 在 VS Code 左侧活动栏新增专属图标，点击调出 Subtree 侧边管理面板
- 侧边面板展示当前工作区所有自动识别的 git subtree 子目录列表，支持折叠展开
- 面板内置快捷按钮：批量同步、全部拉取、全部推送、新增子树、移除子树
- 资源管理器文件夹右键，新增二级菜单「Subtree 操作」

2. 右键菜单功能项
Subtree 操作下拉包含：
- 从远程拉取 Subtree 更新
- 将本地 Subtree 改动推送到远程
- 初始化文件夹为 Git Subtree
- 解绑 Subtree 保留本地文件
- 查看 Subtree 信息

3. 核心能力
- 自动扫描项目目录，识别所有 subtree 目录并列出
- 内部封装原生 git subtree 命令，无需手动敲终端
- 执行命令后在输出面板打印 git 日志、成功失败提示
- 支持单个 subtree 单独操作，也支持批量全部操作
- 不破坏原有 git 仓库结构，保留子目录自身 .git
- 主仓库可正常提交 subtree 内代码，同时子仓库可独立管理

4. 技术要求
- 使用 VS Code 官方扩展开发框架，TypeScript 编写
- 遵循 VS Code 原生 UI 交互风格，布局规整
- 提供完整项目结构、配置文件、所有源码
- 附带编译、打包、本地安装步骤说明
- 代码加注释，可后续自行扩展修改功能